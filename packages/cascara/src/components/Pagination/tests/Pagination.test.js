// import React from 'react';
// import { render } from '@testing-library/react';
// import Pagination from './Pagination';

describe('Pagination', () => {
  test.todo(
    'the component will reset the current page to 1 if the records per page changes'
  );
  test.todo('the next button is disabled if we are at the last page');
  test.todo('the prev button is disabled if we are at the first page');
});
