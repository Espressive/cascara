# TableLayout

`<TableLayout endpoint={} dataConfig={} />` will contain the following:

- `<ColumnControls />`
- `<Filter />`
- `<Pagination />`
- `<Table />`

There may be other components inside we include later
