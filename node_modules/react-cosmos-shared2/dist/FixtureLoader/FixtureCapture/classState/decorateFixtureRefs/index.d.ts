import React from 'react';
import { CachedRefHandlers } from '../shared';
declare type SpyRef = (elPath: string, elRef: null | React.Component) => unknown;
export declare function decorateFixtureRefs(fixture: React.ReactNode, spyRef: SpyRef, cachedRefHandlers: CachedRefHandlers): React.ReactNode;
export {};
