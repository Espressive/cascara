import React from 'react';
import { FixtureState, SetFixtureState } from '../fixtureState';
import { ReactDecorator, ReactDecoratorsByPath } from '../react';
import { FixtureId } from '../renderer';
declare type Props = {
    fixtureId: FixtureId;
    fixture: React.ReactNode;
    systemDecorators: ReactDecorator[];
    userDecorators: ReactDecoratorsByPath;
    fixtureState: FixtureState;
    setFixtureState: SetFixtureState;
    renderMessage?: (args: {
        msg: string;
    }) => React.ReactNode;
    onErrorReset: () => unknown;
};
export declare function FixtureProvider({ fixtureId, fixture, systemDecorators, userDecorators, fixtureState, setFixtureState, onErrorReset, }: Props): JSX.Element;
export {};
