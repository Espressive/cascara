import { FixtureStateData } from '../../fixtureState';
import { SetValue } from './shared';
declare type Opts<T extends FixtureStateData> = {
    defaultValue: T;
};
export declare function useValue<T extends FixtureStateData>(inputName: string, { defaultValue }: Opts<T>): [T, SetValue<T>];
export {};
