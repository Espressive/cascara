"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.collapseOuterDirs = collapseOuterDirs;

function collapseOuterDirs(treeNode) {
  var children = treeNode.children;
  if (!children) return treeNode;
  var childNames = Object.keys(children);
  var containsSingleChild = Object.keys(children).length === 1;

  if (containsSingleChild) {
    var singleChildNode = children[childNames[0]];
    if (singleChildNode.data.type === 'fileDir') return collapseOuterDirs(singleChildNode);
  }

  return treeNode;
}