declare type Props = {
    disabled?: boolean;
    selected: boolean;
    onToggle: () => unknown;
};
export declare function ToggleNavButton({ disabled, selected, onToggle }: Props): JSX.Element;
export {};
