import { FixtureState } from 'react-cosmos-shared2/fixtureState';
import { PluginContext } from 'react-plugin';
import { StorageSpec } from '../Storage/public';
import { ResponsivePreviewSpec, Viewport } from './public';
export declare type ResponsivePreviewContext = PluginContext<ResponsivePreviewSpec>;
export declare type StorageMethods = StorageSpec['methods'];
export declare type FixtureStateWithViewport = FixtureState & {
    viewport?: Viewport;
};
export declare type ViewportState = {
    enabled: boolean;
    scaled: boolean;
    viewport: Viewport;
};
export declare const DEFAULT_DEVICES: {
    label: string;
    width: number;
    height: number;
}[];
export declare const VIEWPORT_STORAGE_KEY = "responsiveViewportState";
export declare const DEFAULT_VIEWPORT_STATE: ViewportState;
