export declare type Viewport = {
    width: number;
    height: number;
};
export declare type Device = Viewport & {
    label: string;
};
export declare type ResponsivePreviewSpec = {
    name: 'responsivePreview';
    config: {
        devices: Device[];
    };
};
