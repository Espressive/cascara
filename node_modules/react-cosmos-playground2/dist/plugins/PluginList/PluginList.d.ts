export declare type SimplePlugin = {
    name: string;
    enabled: boolean;
};
declare type Props = {
    plugins: SimplePlugin[];
    enable: (pluginName: string, enabled: boolean) => void;
};
export declare function PluginList({ plugins, enable }: Props): JSX.Element;
export {};
