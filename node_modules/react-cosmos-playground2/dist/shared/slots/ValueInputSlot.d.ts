import { ReactNode } from 'react';
import { PrimitiveData } from 'react-cosmos-shared2/fixtureState';
import { LeafValue } from '../valueInputTree/shared';
export declare type ValueInputSlotProps = {
    id: string;
    name: string;
    value: LeafValue;
    indentLevel: number;
    onChange: (data: PrimitiveData) => unknown;
};
declare type Props = {
    children: ReactNode;
    slotProps: ValueInputSlotProps;
};
export declare function ValueInputSlot({ children, slotProps }: Props): JSX.Element;
export {};
