import { PrimitiveData } from 'react-cosmos-shared2/fixtureState';
import { LeafValue } from '../shared';
declare type Props = {
    value: LeafValue;
    name: string;
    id: string;
    indentLevel: number;
    onChange: (data: PrimitiveData) => unknown;
};
export declare function ValueInput({ value, name, id, indentLevel, onChange }: Props): JSX.Element;
export {};
