declare type Props = {
    value: string;
    bgColor: string;
    textColor: string;
    size?: number;
    fontSize?: number;
};
export declare function KeyBox({ value, bgColor, textColor, size, fontSize, }: Props): JSX.Element;
export {};
