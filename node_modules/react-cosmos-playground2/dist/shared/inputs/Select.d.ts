declare type BaseOption = {
    value: string;
    label: string;
};
declare type Props<Option extends BaseOption> = {
    id?: string;
    testId?: string;
    options: Option[];
    value: string;
    color: string;
    height: number;
    padding: number;
    onChange: (newValue: Option) => unknown;
};
export declare function Select<Option extends BaseOption>({ id, testId, options, value, color, height, padding, onChange, }: Props<Option>): JSX.Element;
export {};
