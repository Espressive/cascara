/// <reference types="node" />
import http from 'http';
import { BuildMessage } from 'react-cosmos-shared2/build';
export declare function createMessageHandler(httpServer: http.Server): {
    sendMessage: (msg: BuildMessage) => void;
    cleanUp: () => void;
};
