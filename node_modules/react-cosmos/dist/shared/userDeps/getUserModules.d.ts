import { ReactFixtureExportsByPath, ReactDecoratorsByPath } from 'react-cosmos-shared2/react';
import { CosmosConfig } from '../../config';
declare type UserModules = {
    fixtureExportsByPath: ReactFixtureExportsByPath;
    decoratorsByPath: ReactDecoratorsByPath;
};
export declare function getUserModules({ rootDir, fixturesDir, fixtureFileSuffix, }: CosmosConfig): UserModules;
export {};
