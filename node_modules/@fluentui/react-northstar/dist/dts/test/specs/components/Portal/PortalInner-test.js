"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var tslib_1 = require("tslib");
var React = require("react");
var PortalInner_1 = require("src/components/Portal/PortalInner");
var utils_1 = require("test/utils");
var mountPortalInner = function (props) {
    return utils_1.mountWithProvider(React.createElement(PortalInner_1.PortalInner, tslib_1.__assign({}, props),
        React.createElement("p", null)));
};
describe('PortalInner', function () {
    describe('render', function () {
        it('calls react createPortal', function () {
            var mountNode = document.createElement('div');
            var comp = mountPortalInner({ mountNode: mountNode });
            expect(mountNode.contains(comp.getDOMNode())).toBeTruthy();
        });
    });
    describe('onMount', function () {
        it('called when mounting', function () {
            var onMount = jest.fn();
            mountPortalInner({ onMount: onMount });
            expect(onMount).toHaveBeenCalledTimes(1);
        });
    });
    describe('onUnmount', function () {
        it('is called only once when unmounting', function () {
            var onUnmount = jest.fn();
            var wrapper = mountPortalInner({ onUnmount: onUnmount });
            wrapper.unmount();
            expect(onUnmount).toHaveBeenCalledTimes(1);
        });
    });
});
