import { Accessibility } from '@fluentui/accessibility';
import { ComponentWithAs } from '@fluentui/react-bindings';
import { UIComponentProps, ChildrenComponentProps, ContentComponentProps, SizeValue } from '../../utils';
import { FluentComponentStaticProps } from '../../types';
export interface AvatarLabelProps extends UIComponentProps, ChildrenComponentProps, ContentComponentProps {
    /**
     * Accessibility behavior if overridden by the user.
     */
    accessibility?: Accessibility<never>;
    /** The AvatarLabel can have a square shape. */
    square?: boolean;
    /** The AvatarLabel can be circular. */
    circular?: boolean;
    /** Size multiplier. */
    size?: SizeValue;
}
export declare type AvatarLabelStylesProps = Pick<AvatarLabelProps, 'size' | 'square' | 'circular'>;
export declare const avatarlabelClassName = "ui-label";
/**
 * A AvatarLabel allows user to classify content.
 */
export declare const AvatarLabel: ComponentWithAs<'span', AvatarLabelProps> & FluentComponentStaticProps<AvatarLabelProps>;
