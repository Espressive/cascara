import { ComponentWithAs } from '@fluentui/react-bindings';
import { UIComponentProps } from '../../utils';
import { FluentComponentStaticProps } from '../../types';
import { Accessibility } from '@fluentui/accessibility';
export interface AvatarStatusIconProps extends UIComponentProps {
    /** Accessibility behavior if overridden by the user. */
    accessibility?: Accessibility<never>;
    /** The pre-defined state values which can be consumed directly. */
    state?: 'success' | 'info' | 'warning' | 'error' | 'unknown';
}
export declare type AvatarStatusIconStylesProps = Required<Pick<AvatarStatusIconProps, 'state'>>;
export declare const avatarStatusIconClassName = "ui-avatar__statusicon";
/**
 * A AvatarStatusIcon provides a status icon for the Avatar.
 */
export declare const AvatarStatusIcon: ComponentWithAs<'span', AvatarStatusIconProps> & FluentComponentStaticProps;
