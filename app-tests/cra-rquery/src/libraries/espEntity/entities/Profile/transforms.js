export const transformRequest = function (data, headers) {
  // Do whatever you want to transform the data

  return data;
};

export const transformResponse = function (data) {
  // Do whatever you want to transform the data

  return data;
};
